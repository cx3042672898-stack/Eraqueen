# eraQueenA+ 调教版 — 项目结构傻瓜式说明文档

> 这是一份面向非程序员的"傻瓜式"文档，让你知道每个文件是干什么的、改什么去哪里找。

---

## 📁 整体文件结构

```
Eraqueen_v4/
├── index.html          ← 【主文件】整个游戏的页面、弹窗、核心脚本都在这里
├── css/                ← 样式文件夹（控制游戏外观、颜色、布局）
│   ├── style.css       ← 主样式：按钮、卡片、进度条、弹窗等
│   ├── enhance.css     ← 增强样式：打工/闲逛剧情弹窗、商店等
│   ├── themes.css      ← 主题颜色变量（暗夜胭脂、原木米白等）
│   ├── emotion.css     ← 角色情绪系统样式
│   └── personality.css ← 角色性格系统样式
├── js/                 ← JavaScript脚本文件夹（控制游戏逻辑）
│   ├── core/           ← 核心功能代码
│   └── data/           ← 数据文件（角色、道具、剧情等）
├── cg/                 ← AI对话用的提示词模板文件夹
└── PLAN.md / FINAL_GUIDE.md ← 旧的开发记录
```

---

## 📄 核心文件详解

### 1. `index.html` — 游戏主文件（最重要！）

这个文件包含了：
- **页面结构**：首页、庄园、训练、外出、设置等所有界面
- **玩家信息卡**（首页顶部）：头像、名字、性别、体力、经验
- **功能菜单**（首页）：奴隶采购、个人状态、购买道具等按钮
- **剧情记录区**：打工/闲逛/日常/休息/约会/劳役剧情的入口
- **底部导航栏**：首页、庄园、训练、外出、设置
- **所有弹窗**：商店、背包、存档、头像选择、作弊等
- **内联脚本**：玩家档案(\_playerProfile)、头像系统、字体设置等

**你想改首页布局？改功能菜单按钮？** → 来这个文件

#### 关键位置速查：
| 内容 | 大约位置 | 搜索关键字 |
|------|---------|-----------|
| 玩家信息卡 | 第97~115行 | `player-card` |
| 功能菜单 | 第118~162行 | `func-grid` |
| 剧情记录 | 第170~202行 | `story-log-btns` |
| 训练界面 | 第247~319行 | `s-play` |
| 外出界面 | 第322~360行 | `s-outing` |
| 设置页面 | 第363~413行 | `s-set` |
| 底部导航 | 第418~424行 | `<nav id="nav">` |
| 玩家档案变量 | 第771行 | `_playerProfile` |
| 渲染玩家卡片 | 第773行 | `renderPlayerCard` |

---

### 2. `js/core/` — 核心逻辑文件

#### `state.js` — 游戏全局状态
- `State` 对象：存储当前角色、金币、天数、背包等
- `writeSave()` / `loadSave()`：存档和读档
- `getMaxStamina()` / `getMaxEnergy()`：角色体力/精力上限计算
- 升级系统：`checkStatLevelUp()`、`clampStat()`

**你想改初始金币？改角色体力上限公式？** → 来这个文件

#### `game.js` — 角色列表+训练+成就
- `renderChars()`：渲染角色选择列表
- `selectChar()`：选中角色进入训练
- `_renderPlayCore()`：渲染训练界面（角色档案、状态条、指令）
- `setStat()`：更新属性条显示
- `renderActs()`：渲染指令按钮
- `doRest()`：执行休息（恢复所有角色体力）
- `openSlaveExpBars()`：打开角色经验面板
- `openCharDetail()`：打开攻略进度面板
- `ACH_DEFS`：成就定义列表

**你想改训练界面布局？改成就条件？改角色经验显示？** → 来这个文件

#### `features.js` — 功能大杂烩（最大的文件！）
这个文件包含了大量功能，按顺序：

| 功能 | 函数/变量名 | 说明 |
|------|-----------|------|
| 金币流水 | `_moneyLog`、`openMoneyLog()` | 记录每笔收支 |
| 日期计算 | `dayToDate()`、`getDateStr()` | 天数→月日 |
| 体力系统 | `consumeTime()`、`logStaminaCost()` | 时间推进、体力消耗 |
| 经验系统 | `EXP_FIELDS`、`openExpDetail()` | 主角经验详情面板 |
| 个人状态 | `openStatusPanel()` | 个人状态弹窗 |
| 洗脑催眠 | `openHypnosis()`、`BRAINWASH_TYPES` | 催眠系统 |
| 剧情记录 | `openStoryLogModal()`、`pushStoryLog()` | 剧情日志管理 |
| 日常事件 | `PLAYER_DAILY`、`openDailyEventList()` | 日常活动列表 |
| 道具使用 | `openItemUse()`、自用剧情 | 使用道具的故事 |
| 奴隶采购 | `openSlaveProcurement()` | 购买新角色 |
| 庄园系统 | `renderManor()` | 已购奴隶列表 |
| 存档菜单 | `openSaveMenu()` | 存档/读档管理 |
| 作弊面板 | `openCheatPanel()` | 作弊功能 |
| 节日横幅 | `checkFestivalWarning()` | 节日提示系统 |

**你想加新的日常事件？改剧情记录样式？加新功能？** → 大概率在这个文件

#### `shop.js` — 商店系统
- `openShop()`：打开商店
- `buyItem()`：购买道具（含体力/精力药水特殊效果）
- `openBag()`：打开背包
- `shopPreview()`：道具详情预览

#### `outing.js` — 外出系统
- `renderWorkPanel()`：打工地点列表
- `startWork()`：开始打工
- `doWander()`：开始闲逛
- `renderDatePanel()`：约会面板
- `startDate()`：开始约会
- `renderCorveePanel()`：劳役面板
- `openCorvee()`：开始劳役
- `pickWorkChoice()`：选择选项后的结算逻辑
- `finishWork()`：结束后保存记录

**你想加新的打工地点？改约会触发条件？** → 来这个文件

#### `api.js` — AI对话接口
- 连接 Gemini/Claude/OpenAI/DeepSeek 的API设置

#### `story.js` — 剧情弹窗
- `openStoryModal()`：通用剧情展示弹窗

#### `calendar.js` — 日历系统
#### `emotion.js` — 角色情绪系统
#### `personality.js` — 角色性格系统
#### `pregnancy.js` — 孕育系统
#### `relationship.js` — 人际关系+偷情+拍卖系统

---

### 3. `js/data/` — 数据文件

#### `chars_trains.js` — 角色训练数据（基础框架）
#### `items.js` — 商店道具数据库 ⭐
**你想加新道具？** → 来这个文件！

每个道具的格式：
```javascript
{ id: 数字或字符串,
  name: '道具名',
  price: 价格,
  category: 'standard'(永久)/'consumable'(消耗)/'special'(特殊),
  desc: '道具描述',
  npcSay: ['店员说的话1', '店员说的话2', ...]
}
```

#### `work_events.js` — 打工+闲逛+劳役剧情 ⭐⭐
**你想加新的打工/闲逛剧情？** → 来这个文件！

文件结构：
- `WORK_LOCATIONS`：打工地点列表，每个地点下有 `events` 数组
- `WANDER_EVENTS`：闲逛随机事件
- `CORVEE_TYPES`：劳役类型定义
- `CORVEE_EVENTS`：劳役事件数据
- `REST_STORIES`：休息时的随机剧情

#### `date_stories.js` — 约会剧情 ⭐
**你想加新的约会剧情？** → 来这个文件！

#### `stories.js` — 日常剧情（DAILY_EVENTS）
#### `command_stories.js` — 训练指令剧情
#### `dialogues.js` — 对话文本
#### `cmd_descs.js` — 指令描述文本
#### `event_chains.js` — 事件链系统
#### `item_self_stories.js` — 道具自用剧情 ⭐
#### `reverse_training.js` — 逆推系统
#### `emotion_texts.js` — 情绪文本
#### `personality_texts.js` — 性格文本

---

### 4. `css/` — 样式文件

- `style.css`：主样式，改按钮颜色、卡片间距等来这里
- `enhance.css`：增强样式，打工弹窗、商店界面等
- `themes.css`：各主题的颜色变量（想加新主题来这里）

---

### 5. `cg/` — AI提示词模板

当游戏连接AI时，用于生成对话/剧情的提示词模板。

---

## 🔧 常见修改指南

### 想加一个新道具？
1. 打开 `js/data/items.js`
2. 在 `ITEMS_DATA` 数组末尾（`];` 之前）加一个新对象
3. 如果需要特殊效果，去 `js/core/shop.js` 的 `buyItem()` 函数添加处理

### 想加一段打工剧情？
1. 打开 `js/data/work_events.js`
2. 找到对应的打工地点（如"工地"），在它的 `events` 数组里加一条

### 想加一个日常事件？
1. 打开 `js/core/features.js`
2. 搜索 `PLAYER_DAILY`
3. 在数组里添加新的事件对象

### 想改首页功能按钮？
1. 打开 `index.html`
2. 搜索 `func-grid`
3. 添加/删除 `func-btn` 元素

### 想改商店界面？
1. 外观 → `css/enhance.css`（搜索 `shop-`）
2. 逻辑 → `js/core/shop.js`
3. 商品数据 → `js/data/items.js`

### 想让某个指令增加奴隶经验？
在指令的效果里添加：
```javascript
c.expV = (c.expV || 0) + 3;  // V经验+3
c.expA = (c.expA || 0) + 2;  // A经验+2
// 其他经验键名参见 features.js 的 EXP_FIELDS 数组
```

---

## 📋 经验种类速查表

以下经验种类适用于主角和奴隶（键名一致）：

| 键名 | 中文 | 分组 |
|------|------|------|
| expV | V经验 | 基础 |
| expA | A经验 | 基础 |
| expPeak | 绝顶经验 | 基础 |
| expEjac | 射精经验 | 基础 |
| expSex | 性交经验 | 基础 |
| expCreampie | 内射经验 | 基础 |
| expAnal | 肛射经验 | 基础 |
| expU | U经验 | 基础 |
| expM | M经验 | 基础 |
| expSolo | 自慰经验 | 基础 |
| expOral | 口交经验 | 性癖 |
| expLove | 爱情经验 | 性癖 |
| expBind | 紧缚经验 | 性癖 |
| expTrain | 调教经验 | 日常 |
| ... | （完整列表见 features.js 的 EXP_FIELDS） | |

---

## ⚠️ 注意事项

1. 所有数据存储在浏览器的 `localStorage` 里，清除浏览器数据会丢失存档
2. 文件加载顺序很重要，不要随意调整 `<script>` 标签的顺序
3. 改完文件后刷新浏览器即可看到效果
4. 建议每次改动前备份整个文件夹
